[pytest]
# Run order matters:
#   1. run_agents.py  — generates TCs and bug JSONs
#   2. tests/         — test_generated_tcs.py and test_bugs.py read those files
addopts = -v -s --alluredir=allure-results --clean-alluredir
testpaths = run_agents.py tests
console_output_style = progress
